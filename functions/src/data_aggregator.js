

exports.createPresentationData = async (data) => {
    if (!data.rawFitbitData) {
        // Fetch latest fitbit data.
    }
    // Do the same for android data.

    // Roll them up,

    // Save.
    console.log("roll up data!", data);
};
